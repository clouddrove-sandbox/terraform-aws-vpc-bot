name: Auto approve and merge PRs by dependabot

on:
#  pull_request:
#    types:
#      - labeled
#      - unlabeled
#      - synchronize
#      - opened
#      - edited
#      - ready_for_review
#      - reopened
#      - unlocked
#  pull_request_review:
#    types:
#      - submitted
  check_suite:
    types:
      - completed
  status: {}
#  check_run:
#    types: [ completed ]

jobs:
  autoapprove:
    name: Auto Approve and automerge PR by dependabot
    runs-on: ubuntu-latest
    steps:
      - name: Auto approve
        uses: hmarr/auto-approve-action@v3.2.1
        # Perform the auto approve action only when the PR is raised by dependabot
        if: github.actor == 'theprashantyadav'
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

        # merge only dependabot PR
      - name: automerge
        uses: pascalgn/automerge-action@v0.15.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MERGE_FILTER_AUTHOR: 'theprashantyadav'
          MERGE_METHOD: "merge"
          MERGE_DELETE_BRANCH: "false"
          MERGE_LABELS: "dependencies,automerge"
          MERGE_REQUIRED_APPROVALS: "1"
